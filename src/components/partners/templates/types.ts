
export interface Template {
  id: string;
  name: string;
  description: string;
  category: string;
  is_premium: boolean;
  status: string;
  version: string;
  created_at: string;
  installs: number;
}
